// The analytics dashboard sits on an unguessable path with no links pointing
// to it from the setter-facing form — the only "auth" is that Tom keeps this
// URL to himself.
export const DASHBOARD_PATH = "/hq-ss-2417";
